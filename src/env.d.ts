/** Short git hash of the build, injected from astro.config.mjs. Empty outside a git checkout. */
declare const __COMMIT__: string;
/** Full git hash of the build, for linking to the commit. */
declare const __COMMIT_SHA__: string;
