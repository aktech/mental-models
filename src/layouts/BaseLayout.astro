---
import '@fontsource/instrument-serif/400.css';
import '@fontsource/instrument-serif/400-italic.css';
import '@fontsource-variable/geist-mono';
import '@fontsource/fira-sans/400.css';
import '@fontsource/fira-sans/400-italic.css';
import '@fontsource/fira-sans/500.css';
import '@fontsource/fira-sans/600.css';
import '../styles/global.css';
import { ogSlugForPath, withBase } from '../lib/url';
import { ogPages, REPO_URL, SITE_NAME, SITE_TAGLINE } from '../lib/pages';

const commit = __COMMIT__;
const commitUrl = `${REPO_URL}/commit/${__COMMIT_SHA__}`;

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;

// Social card and canonical URL for this page. Pages without their own card
// (styleguide, lab) fall back to the site card.
const cards = await ogPages();
const wanted = ogSlugForPath(Astro.url.pathname, import.meta.env.BASE_URL);
const ogSlug = cards.some((c) => c.slug === wanted) ? wanted : 'index';
const canonical = new URL(Astro.url.pathname, Astro.site);
const ogImage = new URL(withBase(`/og/${ogSlug}.png`), Astro.site);
const metaDescription = description ?? SITE_TAGLINE;
const isEntry = Astro.url.pathname.includes('/models/');
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="light dark" />
    <title>{title}</title>
    <link rel="icon" href={withBase('/favicon.svg')} type="image/svg+xml" />
    <meta name="description" content={metaDescription} />
    <link rel="canonical" href={canonical} />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:type" content={isEntry ? 'article' : 'website'} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={metaDescription} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={title} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={metaDescription} />
    <meta name="twitter:image" content={ogImage} />
    <script is:inline>
      // Resolve the theme before first paint: an explicit choice wins,
      // otherwise follow the OS. The toggle button and the CSS both read
      // data-theme, so this is the one place the rule lives.
      (function () {
        var theme = null;
        try {
          theme = localStorage.getItem('theme');
        } catch (e) {}
        if (theme !== 'light' && theme !== 'dark') {
          theme = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
        }
        document.documentElement.dataset.theme = theme;
      })();
    </script>
  </head>
  <body>
    <header class="flex items-center justify-between px-5 pt-4 pb-10 sm:px-8">
      <a href={withBase('/')} class="site-name">
        <svg class="site-mark" viewBox="0 0 24 24" aria-hidden="true">
          <path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5" />
          <path d="M9 18h6" />
          <path d="M10 22h4" />
        </svg>
        mental models
      </a>
      <nav class="site-nav" aria-label="Site">
        <a href={withBase('/about/')} class="site-link" aria-current={Astro.url.pathname.startsWith(withBase('/about')) ? 'page' : undefined}>about</a>
        <button id="theme-toggle" type="button" class="theme-btn" aria-label="Toggle light and dark mode">
        <svg class="sun" viewBox="0 0 24 24" aria-hidden="true">
          <circle cx="12" cy="12" r="4" />
          <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41" />
        </svg>
        <svg class="moon" viewBox="0 0 24 24" aria-hidden="true">
          <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" />
        </svg>
        </button>
      </nav>
    </header>
    <main class="px-5 pb-24">
      <slot />
    </main>
    <footer class="site-footer prose mx-auto">
      <hr />
      <p>
        All content copyright &copy; <b>A</b>mit <b>K</b>umar unless otherwise noted. Licensed under{' '}
        <a href="https://creativecommons.org/licenses/by-nc/4.0/">CC BY-NC 4.0</a>. Find me on{' '}
        <a href="https://github.com/aktech">GitHub</a>, or shoot me an <a href="mailto:dtu.amit@gmail.com">email</a>.
        {commit && (
          <span class="site-footer-commit">
            {' '}(<a href={commitUrl}>{commit}</a>)
          </span>
        )}
      </p>
    </footer>
    <script>
      const root = document.documentElement;
      const btn = document.getElementById('theme-toggle');
      btn?.addEventListener('click', () => {
        const next = root.dataset.theme === 'dark' ? 'light' : 'dark';
        root.dataset.theme = next;
        try {
          localStorage.setItem('theme', next);
        } catch {}
      });
      // Follow the OS until the reader picks a side, then stop.
      window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
        try {
          if (localStorage.getItem('theme')) return;
        } catch {}
        root.dataset.theme = e.matches ? 'dark' : 'light';
      });
    </script>
  </body>
</html>
